# EOJ-1004 内存显示 - 逐行详细解释
```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    
    while (cin >> s) {
        // 判断是否包含小数点
        if (s.find('.') != string::npos) {
            // double 类型
            double d = atof(s.c_str());
            unsigned char* p = (unsigned char*)&d;
            
            for (int i = 0; i < sizeof(d); i++) {
                cout << hex << setw(2) << setfill('0') 
                     << (int)p[i] << " ";
            }
            cout << endl;
        } else {
            // int 类型
            int a = atoi(s.c_str());
            unsigned char* p = (unsigned char*)&a;
            
            for (int i = 0; i < sizeof(a); i++) {
                cout << hex << setw(2) << setfill('0') 
                     << (int)p[i] << " ";
            }
            cout << endl;
        }
    }

    return 0;
}
```
---

## 第一段：头文件和命名空间

```cpp
#include <bits/stdc++.h>
using namespace std;
```

- `#include <bits/stdc++.h>`：万能头文件，包含C++所有标准库（iostream, string, iomanip, cstdlib等）
- `using namespace std`：使用标准命名空间，可以直接写 `cout`、`cin`、`string`、`endl` 等，不用加 `std::` 前缀

---

## 第二段：主函数和IO优化

```cpp
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
```

- `ios::sync_with_stdio(false)`：取消C++流（cin/cout）与C标准流（stdio）的同步，提高输入输出速度
- `cin.tie(nullptr)`：解除 `cin` 与 `cout` 的绑定，避免每次执行 `cin` 前自动刷新 `cout` 缓冲区

这两行是竞赛编程的标准优化写法。

---

## 第三段：循环读取输入

```cpp
    string s;
    
    while (cin >> s) {
```

- `string s`：声明一个字符串变量，用于存储每行输入的数值
- `while (cin >> s)`：循环读取输入，直到文件结束（EOF）
  - `cin >> s` 会跳过空白字符（空格、换行、制表符），读取下一个非空白字符串
  - 每行只有一个数值，所以每次读取一行正好

---

## 第四段：判断数据类型

```cpp
        // 判断是否包含小数点
        if (s.find('.') != string::npos) {
```

- `s.find('.')`：在字符串 `s` 中查找小数点 `.` 的位置
- `string::npos`：`find()` 函数在找不到时返回的特殊常量
- `!= string::npos`：如果找到了小数点（位置不是 npos），说明这是一个浮点数

**示例：**
```
"3.14" → find('.') = 1 → != npos → true → double
"3"    → find('.') = npos → false → int
"-1.0" → find('.') = 2 → != npos → true → double
```

---

## 第五段：处理 double 类型

```cpp
            // double 类型
            double d = atof(s.c_str());
            unsigned char* p = (unsigned char*)&d;
```

- `double d = atof(s.c_str())`：将字符串转换为 double 类型
  - `s.c_str()`：将 C++ string 转为 C 风格字符串（`const char*`）
  - `atof()`：ASCII to float/double，将字符串转为 double
  
- `unsigned char* p = (unsigned char*)&d`：
  - `&d`：获取变量 `d` 的起始内存地址，类型是 `double*`
  - `(unsigned char*)`：强制转换为 `unsigned char*` 类型
  - 这样 `p` 就指向 `d` 的第一个字节，可以通过 `p[0]`、`p[1]`... 访问每个字节

**为什么要强制转为 `unsigned char*`？**
- 不能直接用 `double*` 逐字节访问
- `unsigned char*` 可以按字节访问内存，且值范围是 0~255，能正确显示十六进制 00~FF

---

## 第六段：循环输出 double 的每个字节

```cpp
            for (int i = 0; i < sizeof(d); i++) {
                cout << hex << setw(2) << setfill('0') 
                     << (int)p[i] << " ";
            }
            cout << endl;
```

- `for (int i = 0; i < sizeof(d); i++)`：循环 `sizeof(d)` 次，即 double 占用的字节数（通常8次）

- `cout << hex`：设置输出格式为**十六进制**（后续所有整数输出都变成十六进制）

- `setw(2)`：设置输出最小宽度为2（来自 `<iomanip>`，已包含在万能头文件中）

- `setfill('0')`：宽度不足时用字符 `'0'` 填充

- `(int)p[i]`：
  - `p[i]` 是 `unsigned char` 类型，值范围 0~255
  - 直接输出 `unsigned char` 会显示为 ASCII 字符（如 65 显示 'A'）
  - 强制转为 `int` 后输出数字

- `" "`：每个字节后面输出一个空格

- `cout << endl`：该行结束后换行

**输出效果示例：**
```
1f 85 eb 51 b8 1e 09 40 
```

---

## 第七段：处理 int 类型

```cpp
        } else {
            // int 类型
            int a = atoi(s.c_str());
            unsigned char* p = (unsigned char*)&a;
            
            for (int i = 0; i < sizeof(a); i++) {
                cout << hex << setw(2) << setfill('0') 
                     << (int)p[i] << " ";
            }
            cout << endl;
        }
    }
```

- `int a = atoi(s.c_str())`：将字符串转换为 int
  - `atoi()`：ASCII to integer，将字符串转为整数
  - `"123"` → 123，`"-456"` → -456

- 其余逻辑与 double 完全相同，只是 `sizeof(a)` 是4（通常 int 占4字节）

---

## 第八段：程序结束

```cpp
    return 0;
}
```

- `return 0`：程序正常结束，返回0给操作系统

---

## 完整执行示例

### 输入：
```
1
-1
0
3.14
0.0
-1.0
```

### 处理过程：

| 输入 | 判断 | 转换 | sizeof | 输出 |
|------|------|------|--------|------|
| "1" | 无小数点 → int | a=1 | 4 | 01 00 00 00 |
| "-1" | 无小数点 → int | a=-1 | 4 | ff ff ff ff |
| "0" | 无小数点 → int | a=0 | 4 | 00 00 00 00 |
| "3.14" | 有小数点 → double | d=3.14 | 8 | 1f 85 eb 51 b8 1e 09 40 |
| "0.0" | 有小数点 → double | d=0.0 | 8 | 00 00 00 00 00 00 00 00 |
| "-1.0" | 有小数点 → double | d=-1.0 | 8 | 00 00 00 00 00 00 f0 bf |

---

## 关键函数/操作符说明

| 代码 | 功能 | 示例 |
|------|------|------|
| `atof(s.c_str())` | 字符串 → double | `"3.14"` → `3.14` |
| `atoi(s.c_str())` | 字符串 → int | `"123"` → `123` |
| `&d` | 取地址 | 获取变量的内存地址 |
| `(unsigned char*)` | 强制类型转换 | 转为字节指针 |
| `sizeof(d)` | 获取类型大小 | double 通常8字节 |
| `hex` | 设置十六进制输出 | 255 → `ff` |
| `setw(2)` | 设置最小宽度 | 不足2位补空格 |
| `setfill('0')` | 设置填充字符 | 用 `0` 填充空位 |

---
